# ============================================================
# Legal RAG Assistant - Complete Requirements
# ============================================================
# Install with: pip install -r requirements.txt
# Update all:   pip install --upgrade -r requirements.txt
# ============================================================

# ── Streamlit UI ─────────────────────────────────────────────
streamlit>=1.32.0

# ── LangChain Core ───────────────────────────────────────────
langchain>=0.2.0                  # Core abstractions (chains, prompts, retrievers)
langchain-core>=0.2.0             # Base interfaces (used internally by all langchain packages)
langchain-community>=0.2.0        # Document loaders, vector stores (PyPDFLoader, FAISS, Chroma)
langchain-text-splitters>=0.2.0   # RecursiveCharacterTextSplitter and other splitters
langchain-huggingface>=0.0.3      # HuggingFaceEmbeddings (replaces deprecated langchain.embeddings)

# ── Vector Stores ─────────────────────────────────────────────
faiss-cpu>=1.7.4                  # FAISS vector store (use faiss-gpu if you have CUDA)
chromadb>=0.4.0                   # Chroma vector store

# ── Embeddings & Transformers ─────────────────────────────────
sentence-transformers>=2.7.0      # all-MiniLM-L6-v2, all-mpnet-base-v2, etc.
transformers>=4.40.0              # HuggingFace pipeline for text generation
tokenizers>=0.19.0                # Fast tokenizers (required by transformers)
accelerate>=0.30.0                # Speeds up model loading on CPU/GPU
huggingface-hub>=0.23.0           # Model downloads from HuggingFace Hub

# ── PyTorch (CPU) ─────────────────────────────────────────────
# For GPU support replace with: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0

# ── PDF Handling ──────────────────────────────────────────────
pypdf>=4.0.0                      # PDF loading (used by PyPDFLoader)
pdfminer.six>=20221105            # Fallback PDF text extraction
pymupdf>=1.24.0                   # Optional: better PDF parsing (fitz)

# ── Utilities ─────────────────────────────────────────────────
numpy>=1.26.0                     # Numerical operations
tqdm>=4.66.0                      # Progress bars
python-dotenv>=1.0.0              # Load .env files (for API keys if needed)
tiktoken>=0.7.0                   # Token counting (useful for chunking strategies)